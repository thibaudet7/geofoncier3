{
  "version": 2,
  "builds": [
    {
      "src": "api/server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["index.html", "public/**", "api/**"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/server.js" }
  ]
}
